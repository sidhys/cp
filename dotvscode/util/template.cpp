#ifndef SETUP_COLLAPSER_BECAUSE_I_DONT_LIKE_LOOKING_AT_IT
#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define ld long double 
#define vt vector 
#define pb push_back
#define ar array
#define all(_in) (_in).begin(), (_in).end()
#define each(x, a) for (auto& x: a)
#define alert(msg) printf("\033[1;31mMESSAGE: \"%s\" AT LINE %d\033[0m\n", (msg), __LINE__)
#define measure_time(operation, duration) \
    { \
        auto start_##__LINE__ = std::chrono::high_resolution_clock::now(); \
        operation; \
        auto stop_##__LINE__ = std::chrono::high_resolution_clock::now(); \
        duration = duration_cast<std::chrono::microseconds>(stop_##__LINE__ - start_##__LINE__).count(); \
    }
#endif
// ==================================== // 


vt<int> quicksort(vt<int> in) {


int main () {
    ios::sync_with_stdio(0);
    cin.tie(0);
     
    // int n; cin >> n;

    cout << endl; return 0;
}